NUXT_DATABASE_URL=postgresql://tourenplan:tourenplan_dev@127.0.0.1:5433/tourenplan

# Required: min 32 chars. Production rejects this example value.
NUXT_JWT_SECRET=change-me-in-production-min-32-chars

# Local default only. Production: unique password, min 12 chars (defaults are rejected).
NUXT_BOOTSTRAP_ADMIN_PASSWORD=admin-change-me
NUXT_BOOTSTRAP_ADMIN_EMAIL=admin@localhost.local

NUXT_ORS_API_KEY=your-openrouteservice-api-key

# true only behind a trusted reverse proxy (Caddy). docker-compose.prod.yml sets this.
NUXT_TRUST_PROXY=false

POSTGRES_USER=tourenplan
POSTGRES_PASSWORD=tourenplan_dev
POSTGRES_DB=tourenplan

# Production (docker-compose.prod.yml)
DOMAIN=tourenplan.example.com
